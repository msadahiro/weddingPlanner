@{
    ViewData["Title"] = "User";
}
<div id="ShowPerson">
    <div class="header">
        <h1>@ViewBag.ShowPerson.FirstName</h1>
        <a href="/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
    </div>
    <div class="mainContent">
        <h3>Number of weddings RSVP'd to: @ViewBag.ShowPerson.Reserves.Count</h3>
        <table>
            <thead>
                <th>Weddings to Attend</th>
                <th>Action</th>
            </thead>
            <tr></tr>
            <tbody>
                <tr>
                    
                        @{
                            foreach(var wedding in @ViewBag.ShowPerson.Reserves){
                                <td>@wedding.Wedding.SpouseName1 and @wedding.Wedding.SpouseName2's wedding</td>
                            }
                        }
                    
                    <td><a href="">RSVP</a></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

